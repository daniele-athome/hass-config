
intent_script:

  GetExternalTemperature:
    action:
      - service: script.say_external_temperature

script:

  # Pronuncia la temperatura esterna locale attuale
  say_external_temperature:
    sequence:
      - service: script.say_something
        data_template:
          message: !include ../../templates/it/assistant_say_external_temperature.yaml
